# =============================================================================
# Hotel Billing Management Admin Portal - Environment Variables
# =============================================================================
# Copy this file to .env.local for local development
# For production deployment, set these variables in your hosting platform
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (Supabase PostgreSQL)
# -----------------------------------------------------------------------------
# Get this from your Supabase project settings > Database > Connection string
# Format: postgresql://postgres:[YOUR-PASSWORD]@[HOST]:5432/postgres
DATABASE_URL=postgresql://user:password@host:5432/database

# -----------------------------------------------------------------------------
# AWS S3 Configuration
# -----------------------------------------------------------------------------
# AWS region where your S3 buckets are located
AWS_REGION=us-east-1

# AWS IAM credentials with S3 access permissions
# Create an IAM user with S3 read/write permissions
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key

# S3 bucket names (must be globally unique)
# Bucket for hotel profile photos
S3_BUCKET_PHOTOS=hotel-photos-bucket

# Bucket for invoice PDFs
S3_BUCKET_INVOICES=hotel-invoices-bucket

# -----------------------------------------------------------------------------
# JWT Authentication
# -----------------------------------------------------------------------------
# Secret key for signing JWT tokens (use a strong random string)
# Generate with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_here

# Token expiration time (24 hours recommended)
JWT_EXPIRES_IN=24h

# -----------------------------------------------------------------------------
# Hugging Face AI Integration
# -----------------------------------------------------------------------------
# API key from huggingface.co (Settings > Access Tokens)
HUGGINGFACE_API_KEY=your_huggingface_api_key

# Model to use for invoice generation
# Default: facebook/bart-large-cnn (can be changed to other text generation models)
HUGGINGFACE_MODEL=facebook/bart-large-cnn

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Public URL of your application (used for redirects and CORS)
# Development: http://localhost:3000
# Production: https://your-domain.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Node environment (development, production, test)
NODE_ENV=development

# -----------------------------------------------------------------------------
# Optional: Rate Limiting Configuration
# -----------------------------------------------------------------------------
# Maximum requests per window (default: 100)
# RATE_LIMIT_MAX=100

# Rate limit window in minutes (default: 15)
# RATE_LIMIT_WINDOW=15

# -----------------------------------------------------------------------------
# Optional: Puppeteer Configuration (for PDF generation)
# -----------------------------------------------------------------------------
# Set to 'true' in serverless environments like Vercel
# PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true

# Path to Chrome executable (for serverless environments)
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable
